name: Auto Format

on:
  push:
    branches: [ main ]
    paths:
      - '**.swift'
      - '.github/workflows/auto-format.yml'
      - 'Package.swift'

permissions:
  contents: write

jobs:
  format:
    name: Format and Lint
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Apply SwiftLint
        run: |
          brew install swiftlint
          swiftlint Sources/ Tests/
          echo "SwiftLint applied successfully"
          
      - name: Apply SwiftFormat
        run: |
          brew install swiftformat
          swiftformat Sources/ Tests/
          echo "SwiftFormat applied successfully"
      
      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "No formatting changes needed"
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "Formatting changes detected:"
            git diff --stat
          fi
      
      - name: Commit and push changes
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: apply SwiftFormat [skip ci]"
          git push
      
      - name: Summary
        run: |
          echo "## 🎨 SwiftFormat Auto-Applied" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.changes.outputs.has_changes }}" == "true" ]; then
            echo "✅ Formatting changes committed and pushed" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ No formatting changes needed" >> $GITHUB_STEP_SUMMARY
          fi
